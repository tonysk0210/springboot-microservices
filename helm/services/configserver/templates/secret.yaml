{{- if .Values.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
type: Opaque
stringData:
  encrypt-key: {{ required "secret.encryptKey is required when secret.create is true" .Values.secret.encryptKey | quote }}
{{- end }}
