# =============================================================================
#  card.yml —— Config Server 發給 card 服務的設定
# =============================================================================
#
#  ⚠ 檔名必須等於 card/src/main/resources/application.yaml 裡的
#    spring.application.name（= card）。對不上就什麼都拿不到，且沒有錯誤訊息。
#
#  取得方式：
#      http://localhost:8071/card/default
#      http://localhost:8071/card-default.yml
# =============================================================================

card:
  message: "歡迎使用信用卡相關 API（來自 configserver / classpath）"

  contact-details:
    name: "Tony Shangkuan - Developer"
    email: "card-support@eazybank.com"

  on-call-support:
    - "(02) 6666-7777"
    - "(02) 8888-9999"

build:
  version: "1.0"

logging:
  level:
    org.hibernate.tool.schema: DEBUG
