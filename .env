# Docker Compose 會自動讀取本檔案；IntelliJ/Maven 不會自動讀取，啟動前需手動設定：$env:ENCRYPT_KEY="貼上 .env 裡的金鑰"
# 本專案是練習用，檔案會提交 Git；正式環境請放入 .gitignore 並使用真正的 secret 管理。修改後需重啟服務或重新建立容器才會生效。

# 加密流程：
# 1. 先設定 ENCRYPT_KEY 並啟動 Config Server，再呼叫 POST /encrypt 將明文轉成密文。
# 2. 將結果寫成 '{cipher}密文' 放入 Git/native 設定檔
# 3. 微服務啟動或 refresh 取設定時，Config Server 會使用同一把 ENCRYPT_KEY 解密，再把明文回傳給微服務。

# 產生新金鑰（PowerShell）：
# [Convert]::ToHexString([Security.Cryptography.RandomNumberGenerator]::GetBytes(32)).ToLower()
# 更換 ENCRYPT_KEY 後，既有密文將無法解密。

# ENCRYPT_KEY 是 Config Server 的解密金鑰。
ENCRYPT_KEY=560b5cca941e67b3f6a040f1f9c2744508889f5613131b269ff4d16f6c2a3062

# Keycloak 初始管理員；只在資料庫首次建立時生效。
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# MySQL root 密碼；只在 mysql-data volume 第一次建立時生效。
MYSQL_ROOT_PASSWORD=root
